package middleware

import (
	"github.com/gin-gonic/gin"
)

func {{.Name}}() gin.HandlerFunc {
	return func(c *gin.Context) {
		// t := time.Now()

		// 请求前

		c.Next()

		// 请求后

		// latency := time.Since(t)

	}
}